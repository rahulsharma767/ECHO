"use client";import {useEffect,useState} from "react";import {useEchoDataSource} from "@/lib/dataSourceContext";import type {SceneState} from "@/types";export function useSceneTakes(sceneId:string){const source=useEchoDataSource();const [v,setV]=useState<SceneState>({scene_id:sceneId,reference_lines:[],takes:[]});useEffect(()=>source.subscribeSceneTakes(sceneId,setV),[source,sceneId]);return v}
