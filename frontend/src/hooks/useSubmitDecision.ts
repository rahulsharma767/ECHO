"use client";import {useState} from "react";import {useEchoDataSource} from "@/lib/dataSourceContext";import type {HumanDecisionValue} from "@/types";export function useSubmitDecision(){const source=useEchoDataSource();const [submitting,setSubmitting]=useState(false);async function submit(sceneId:string,takeNumber:number,decision:HumanDecisionValue){setSubmitting(true);try{await source.submitHumanDecision(sceneId,takeNumber,decision)}finally{setSubmitting(false)}}return {submit,submitting}}
